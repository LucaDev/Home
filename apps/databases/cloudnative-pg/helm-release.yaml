apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cnpg
  namespace: databases
spec:
  interval: 5m
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.19.0
      sourceRef:
        kind: HelmRepository
        name: cnpg
        namespace: flux-system
      interval: 5m
  values:
