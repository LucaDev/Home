---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mck
  namespace: databases
spec:
  interval: 10m
  chart:
    spec:
      chart: mongodb-kubernetes
      version: 1.4.0
      sourceRef:
        kind: HelmRepository
        name: mongodb
        namespace: flux-system
      interval: 5m
  values:
