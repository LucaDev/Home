---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tf-controller
  namespace: flux-system
spec:
  interval: 30m
  chart:
    spec:
      chart: tf-controller
      version: 0.15.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: tf-controller
  values:
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
        interval: 1m
