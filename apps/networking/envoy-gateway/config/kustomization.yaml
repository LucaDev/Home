---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: networking
resources:
  - ./envoy.yaml
  - ./observability.yaml
  - ./error-5x.yaml
configMapGenerator:
  - name: envoy-500-error
    files:
      - error/500.html
generatorOptions:
  disableNameSuffixHash: true
