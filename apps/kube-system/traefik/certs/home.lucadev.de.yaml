apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-lucadev-de-staging-wildcard
spec:
  secretName: home-lucadev-de-wildcard-staging-cert
  dnsNames:
  - home.lucadev.de
  - "*.home.lucadev.de"
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
