---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &appname esphome
  namespace: flux-system
spec:
  interval: 1h
  path: "./apps/home-automation/esphome/app"
  prune: false # TODO
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false # no flux ks dependents
  dependsOn:
    - name: volsync
    - name: external-secrets-stores
  postBuild:
    substitute:
      APP: *appname
