---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: esphome
  namespace: home-automation
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template-4
    namespace: flux-system
  values:
    controllers:
      esphome:
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"

        statefulset:
          volumeClaimTemplates:
            - name: config
              accessMode: ReadWriteOnce
              size: 2Gi
              storageClass: ssd-singlereplica
              globalMounts:
                - path: /config

        containers:
          app:
            image:
              repository: ghcr.io/esphome/esphome
              tag: 2026.1.5@sha256:0d10dd966bfba46d1e60d6050f7ec33939b1e483482e5f39b349fa18272dd569
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            env:
              ESPHOME_DASHBOARD_USE_PING: "true"
            resources:
              requests:
                cpu: 5m
                memory: 512M
              limits:
                memory: 8192M

    service:
      app:
        controller: esphome
        ports:
          http:
            port: 6052

    persistence:
      secrets:
        type: secret
        name: esphome-secrets
        globalMounts:
          - path: /config/secrets.yaml
            subPath: secrets.yaml
      esphome-cache:
        type: emptyDir
        globalMounts:
          - path: /config/.esphome
      buildcache:
        size: 15Gi
        accessMode: ReadWriteOnce
        storageClass: cached-singlereplica
        globalMounts:
          - path: /cache
    route:
      app:
        hostnames:
          - "esphome.lucadev.de"
        parentRefs:
          - name: envoy
            namespace: networking
            sectionName: https
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/description: Geräte-Verwaltung
          gethomepage.dev/group: Weitere
          gethomepage.dev/icon: esphome
          gethomepage.dev/name: ESPHome
