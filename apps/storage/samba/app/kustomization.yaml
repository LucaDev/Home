apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: storage
resources:
  - helmrelease.yaml
configMapGenerator:
  - name: samba-config
    files:
      - config.json
generatorOptions:
  disableNameSuffixHash: true
