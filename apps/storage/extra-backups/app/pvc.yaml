---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-upload-claim
  labels:
    app: immich-server
    parent: immich
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
