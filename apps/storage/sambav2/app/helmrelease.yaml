---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2beta2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: sambav2
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.1.0
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controllers:
      samba:
        strategy: RollingUpdate

        containers:
          app:
            image:
              repository: ghcr.io/lucadev/samba
              tag: 4.19.5@sha256:af00a12cca5ea4f2d3460a9355a26490cd3386bc9fdfc24b9a240c0daded78b9
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
            envFrom:
              - secretRef:
                  name: samba-ldap
          wsdd:
            image:
              repository: ghcr.io/lucadev/wsdd2
              tag: 1.0.31
            resources:
              requests:
                cpu: 5m
                memory: 25Mi
        annotations:
          reloader.stakater.com/auto: "true"

    service:
      app:
        type: LoadBalancer
        controller: samba
        ports:
          smb:
            port: 4455
            protocol: TCP
          multicast:
            port: 3702
            protocol: UDP
          http:
            port: 3702
            protocol: TCP
          llmnr-multicast:
            port: 5355
            protocol: TCP
          llmnr-unicast:
            port: 5355
            protocol: UDP

    persistence:
      config-file:
        type: configMap
        name: samba2-config
        globalMounts:
          - path: /etc/smb.d/config.conf
            subPath: config.conf
            readOnly: true
      homes:
        type: emptyDir
        globalMounts:
          - path: /homes
      public:
        type: emptyDir
        globalMounts:
          - path: /public
