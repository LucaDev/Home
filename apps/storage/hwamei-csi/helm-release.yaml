---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hwameistor-operator
  namespace: storage
spec:
  interval: 30m
  chart:
    spec:
      chart: hwameistor-operator
      version: 0.10.4
      sourceRef:
        kind: HelmRepository
        name: hwameistor
        namespace: flux-system
      interval: 30m
