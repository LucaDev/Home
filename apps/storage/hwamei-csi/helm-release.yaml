---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hwameistore-operator
  namespace: storage
spec:
  interval: 30m
  chart:
    spec:
      chart: hwameistore-operator
      version: 0.13.2
      sourceRef:
        kind: HelmRepository
        name: hwameistor
        namespace: flux-system
      interval: 30m
