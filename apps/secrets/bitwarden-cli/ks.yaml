---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: bitwarden-cli
  namespace: flux-system
spec:
  interval: 1h
  path: "./apps/secrets/bitwarden-cli/app"
  prune: false # TODO
  sourceRef:
    kind: GitRepository
    name: github-repo
  wait: true
