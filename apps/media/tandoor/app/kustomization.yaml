---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - ./helmrelease.yaml
  - ./externalsecret.yaml
components:
  - ../../../../components/volsync/hetzner
configMapGenerator:
  - name: tandoor-nginx-configmap
    files:
      - ./config/nginx-config
generatorOptions:
  disableNameSuffixHash: true
